# require	forbid	label	task
api-design	-	compat-api	Plan a public REST API response-field rename while old and new clients may run concurrently, migrating without breaking old clients.
api-design ui-ux-baseline	-	compat-api-ui	Rename a response field on an existing REST endpoint used by a React admin status view. Plan consumer discovery, API and UI migration, and user-flow verification to prevent drift.
-	ui-ux-baseline	compat-api-no-ui	Add an endpoint for internal service-to-service health metadata, confirming there is no frontend consumer and the scope is API-only.
data-design	-	compat-schema	Plan a required database column addition during a rolling deployment where old and new code coexist, including backfill and later contraction.
ops	-	compat-infra	Plan a change to an infrastructure resource shared by old and new revisions during rollout, including dependencies, preconditions, and consumer verification.
-	api-design data-design ops	compat-local	Rename a local variable within one function, preserving behavior with no public, data, or deployment contract.
